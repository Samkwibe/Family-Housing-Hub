version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
    name: 'frontend-artifacts'
  cache:
    paths:
      - node_modules/**/*
